#!/usr/bin/env python

from distutils.core import setup

setup(name='Django-Performance-Middleware',
      version='1.0',
      description='A tool to track down slowness in your django app',
      install_requires=['Django==1.4'],
     )
